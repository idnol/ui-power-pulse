export const selectSuccess = state => state.diary.isSuccess;

export const selectError = state => state.diary.error;

export const selectorDate = state => state.diary.selectedDate;

export const selectProducts = state => state.diary.products;

